---
- hosts: all
  connection: local
  sudo: yes
  vars_files:
    - /vagrant/vars/config.yml

  roles:
    - { role: openconnect, vpn_url: "{{ server }}", vpn_user: "{{ user }}", vpn_password: "{{ password }}" }