description "openvpn vpn client daemon"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 5 120

exec openvpn --config "/vagrant/{{ config_directory }}/{{ vpn_config }}" --cd "/vagrant/{{ config_directory }}"
